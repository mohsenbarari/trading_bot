{
  "schema_version": 2,
  "required_invariant_classes": [
    "identity_guard",
    "request_server_guard",
    "invitation_idempotency",
    "intent_dedupe",
    "otp_atomic_consume",
    "lock_order",
    "expiry_boundary",
    "source_field_allowlist",
    "projection_gate",
    "otp_claim_timing",
    "background_server_guard",
    "sms_category_policy",
    "transaction_boundary"
  ],
  "targets": [
    {"id": "MUT-IDENTITY-SQL-GUARD", "invariant_class": "identity_guard", "module": "core.registration_identity", "function": "_checked_sql_identifier", "matcher": "core.registration_identity.x__checked_sql_identifier*"},
    {"id": "MUT-REG-REQUEST-SHAPE", "invariant_class": "request_server_guard", "module": "core.services.authoritative_registration_service", "function": "_validate_request_shape", "matcher": "core.services.authoritative_registration_service.x__validate_request_shape*"},
    {"id": "MUT-INV-EXACT-RETRY", "invariant_class": "invitation_idempotency", "module": "core.services.canonical_invitation_creation_service", "function": "_is_exact_base_retry", "matcher": "core.services.canonical_invitation_creation_service.x__is_exact_base_retry*"},
    {"id": "MUT-INTENT-MATCH", "invariant_class": "intent_dedupe", "module": "core.services.telegram_registration_intent_service", "function": "_intent_matches_command", "matcher": "core.services.telegram_registration_intent_service.x__intent_matches_command*"},
    {"id": "MUT-OTP-CONSUME", "invariant_class": "otp_atomic_consume", "module": "core.services.otp_delivery_state_service", "function": "consume_otp_code", "matcher": "core.services.otp_delivery_state_service.x_consume_otp_code*"},
    {"id": "MUT-REG-LOCK-ORDER", "invariant_class": "lock_order", "module": "core.services.registration_command_receipt_service", "function": "registration_command_lock_keys", "matcher": "core.services.registration_command_receipt_service.x_registration_command_lock_keys*"},
    {"id": "MUT-INV-EXPIRY", "invariant_class": "expiry_boundary", "module": "core.services.authoritative_registration_service", "function": "_validate_invitation_time", "matcher": "core.services.authoritative_registration_service.x__validate_invitation_time*"},
    {"id": "MUT-SYNC-FIELD-POLICY", "invariant_class": "source_field_allowlist", "module": "core.registration_sync_policy", "function": "allowed_user_fields_for_source", "matcher": "core.registration_sync_policy.x_allowed_user_fields_for_source*"},
    {"id": "MUT-PROJECTION-GATE", "invariant_class": "projection_gate", "module": "core.services.telegram_registration_intent_service", "function": "registration_projection_is_ready", "matcher": "core.services.telegram_registration_intent_service.x_registration_projection_is_ready*"},
    {"id": "MUT-OTP-CLAIM", "invariant_class": "otp_claim_timing", "module": "core.services.otp_delivery_state_service", "function": "claim_sms_delivery", "matcher": "core.services.otp_delivery_state_service.x_claim_sms_delivery*"},
    {"id": "MUT-JOB-SERVER-GUARD", "invariant_class": "background_server_guard", "module": "core.background_job_authority", "function": "check_background_job_authority", "matcher": "core.background_job_authority.x_check_background_job_authority*"},
    {"id": "MUT-SMS-CATEGORY-POLICY", "invariant_class": "sms_category_policy", "module": "core.invitation_sms_policy", "function": "invitation_sms_enabled", "matcher": "core.invitation_sms_policy.x_invitation_sms_enabled*"},
    {"id": "MUT-REG-COMMIT-BOUNDARY", "invariant_class": "transaction_boundary", "module": "core.services.authoritative_registration_service", "function": "_commit_result", "matcher": "core.services.authoritative_registration_service.x__commit_result*"}
  ],
  "equivalent_mutants": [
    {"name": "core.services.registration_command_receipt_service.x_registration_command_lock_keys__mutmut_5", "reason": "Python treats UTF-8 and utf-8 as the same codec, so only the case of the encoding label changes."},
    {"name": "core.registration_sync_policy.x_allowed_user_fields_for_source__mutmut_5", "reason": "For every falsy source, both the empty-string and sentinel fallback normalize to an unknown source and return the same empty frozenset."},
    {"name": "core.services.canonical_invitation_creation_service.x__is_exact_base_retry__mutmut_12", "reason": "Invitation always defines created_by_id; changing getattr's missing-attribute default cannot affect a model instance."},
    {"name": "core.services.canonical_invitation_creation_service.x__is_exact_base_retry__mutmut_15", "reason": "Invitation always defines created_by_id; omitting getattr's default is behaviorally identical."},
    {"name": "core.services.canonical_invitation_creation_service.x__is_exact_base_retry__mutmut_18", "reason": "Invitation always defines created_by_id; changing only the missing-attribute default cannot affect valid input."},
    {"name": "core.services.canonical_invitation_creation_service.x__is_exact_base_retry__mutmut_27", "reason": "Invitation always defines kind; omitting getattr's default is behaviorally identical."},
    {"name": "core.services.canonical_invitation_creation_service.x__is_exact_base_retry__mutmut_36", "reason": "Invitation always defines role; omitting getattr's default is behaviorally identical."},
    {"name": "core.services.canonical_invitation_creation_service.x__is_exact_base_retry__mutmut_43", "reason": "Invitation always defines mobile_number; changing only the missing-attribute default cannot affect valid input."},
    {"name": "core.services.canonical_invitation_creation_service.x__is_exact_base_retry__mutmut_46", "reason": "Invitation always defines mobile_number; omitting getattr's default is behaviorally identical."},
    {"name": "core.services.canonical_invitation_creation_service.x__is_exact_base_retry__mutmut_49", "reason": "Invitation always defines mobile_number; changing only the missing-attribute default cannot affect valid input."},
    {"name": "core.services.canonical_invitation_creation_service.x__is_exact_base_retry__mutmut_54", "reason": "Invitation always defines account_name; changing only the missing-attribute default cannot affect valid input."},
    {"name": "core.services.canonical_invitation_creation_service.x__is_exact_base_retry__mutmut_57", "reason": "Invitation always defines account_name; omitting getattr's default is behaviorally identical."},
    {"name": "core.services.canonical_invitation_creation_service.x__is_exact_base_retry__mutmut_60", "reason": "Invitation always defines account_name; changing only the missing-attribute default cannot affect valid input."},
    {"name": "core.services.telegram_registration_intent_service.x__intent_matches_command__mutmut_22", "reason": "TelegramRegistrationCommand.model_dump always contains every snapshot key, so pop(key) and pop(key, None) are equivalent."},
    {"name": "core.services.telegram_registration_intent_service.x__intent_matches_command__mutmut_25", "reason": "TelegramRegistrationCommand.model_dump always contains every snapshot key, so pop(key) and pop(key, None) are equivalent."},
    {"name": "core.invitation_sms_policy.x_invitation_sms_enabled__mutmut_13", "reason": "For a falsy invitation kind, both the empty string and sentinel normalize to an unknown kind and the function returns False without reading a category flag."},
    {"name": "core.invitation_sms_policy.x_invitation_sms_enabled__mutmut_31", "reason": "For a falsy customer tier, both the empty string and sentinel normalize to an unknown tier and the function returns False without reading a tier flag."},
    {"name": "core.services.telegram_registration_intent_service.x_registration_projection_is_ready__mutmut_49", "reason": "Invitation is a mapped model with registered_user_id always present; changing only getattr's missing-attribute default cannot affect a loaded row."},
    {"name": "core.services.telegram_registration_intent_service.x_registration_projection_is_ready__mutmut_52", "reason": "Invitation is a mapped model with registered_user_id always present; omitting getattr's default is behaviorally identical for loaded rows."},
    {"name": "core.services.telegram_registration_intent_service.x_registration_projection_is_ready__mutmut_55", "reason": "Invitation is a mapped model with registered_user_id always present; changing only getattr's missing-attribute default cannot affect a loaded row."},
    {"name": "core.services.telegram_registration_intent_service.x_registration_projection_is_ready__mutmut_56", "reason": "A valid completed Invitation has a positive registered_user_id; for invalid null or zero rows both fallback values reject every positive User id before projection."},
    {"name": "core.services.telegram_registration_intent_service.x_registration_projection_is_ready__mutmut_83", "reason": "Changing the duplicate-relation OR to AND cannot change the result: each supported kind's downstream cardinality guard independently rejects the remaining duplicate set."},
    {"name": "core.services.telegram_registration_intent_service.x_registration_projection_is_ready__mutmut_84", "reason": "A single accountant relation is already rejected by every supported projection kind downstream, so changing the early duplicate threshold has no observable effect."},
    {"name": "core.services.telegram_registration_intent_service.x_registration_projection_is_ready__mutmut_85", "reason": "Two accountant relations are rejected by each supported kind's downstream relation guard even when the early threshold changes from greater-than-one to greater-than-two."},
    {"name": "core.services.telegram_registration_intent_service.x_registration_projection_is_ready__mutmut_87", "reason": "Two customer relations are rejected by each supported kind's downstream relation guard even when the early threshold changes from greater-than-one to greater-than-two."},
    {"name": "core.services.telegram_registration_intent_service.x_registration_projection_is_ready__mutmut_93", "reason": "Invitation.kind is a non-null mapped InvitationKind enum on loaded rows, so changing only getattr's missing-attribute fallback cannot affect supported input."}
  ],
  "ignores": [
    {"selector": "generated migrations", "reason": "Immutable migration history is covered by migration execution and schema parity tests."},
    {"selector": "framework bootstrap", "reason": "Import and server bootstrap wiring has no isolated invariant boundary."},
    {"selector": "logging-only formatting", "reason": "Redaction and audit contracts are covered by dedicated semantic tests."}
  ]
}
