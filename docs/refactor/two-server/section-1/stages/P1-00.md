# Task Card `P1-00`

```text
status: IN_PROGRESS
approved_by: pending owner review
approved_at: pending
base_branch: plan/two-server-refactor-v1
base_commit: b8333ea4
```

## Goal / non-goals

Goal: create an evidence-backed current-state and behavior baseline before any
Finland consolidation implementation.

Non-goals: provisioning, refactor, migration, deploy, restart, DNS, runtime
write, production parity-status write, cleanup or bug fix.

## Dependencies and evidence

- Owner authorized read-only SSH/runtime inspection in the preceding planning
  discussion.
- The master plan itself remains `DRAFT`; therefore this Task Card cannot self-
  approve its human gate or unblock implementation.
- Repository preflight found one clean canonical worktree on the plan branch.

## Files allowed to change

```text
docs/TWO_SERVER_REFACTOR_MASTER_PLAN.md
docs/architecture/FINLAND_PRIMARY_TARGET.md
docs/refactor/two-server/section-1/**
docs/memory/areas/infrastructure.md
```

No application, migration, deploy script, configuration or test behavior is in
scope.

## External systems allowed to read or write

| System | Read | Write |
| --- | --- | --- |
| current Bot-Finland host/runtime | approved | forbidden |
| current Web-Finland host/runtime | approved | forbidden |
| new Finland target | approved | forbidden |
| PostgreSQL/Redis/containers/systemd | status/query only | forbidden |
| Telegram/SMS/Arvan/Object Storage | not required | forbidden |

## Pre-change snapshot

- plan branch at `b8333ea4` and clean worktree;
- runtime release `e533d415...` on both current roles;
- application schema `ff6c7d8e9f01` on both current roles;
- target accessible but empty/unprovisioned;
- no runtime write made by the audit.

## Ordered execution steps

1. read project memory and plan contracts;
2. inventory repository/static code surfaces;
3. inspect the two current hosts and target read-only;
4. classify all model tables and background jobs;
5. run a complete read-only shared-table parity comparison;
6. inspect Web/Bot provenance/policy code;
7. run focused side-effect-free characterization tests;
8. write dossier, inventory, ownership, policy, parity, drift and cleanup artifacts;
9. validate Markdown/JSON/checksums and request owner review.

## Tests

| Class | Evidence/result |
| --- | --- |
| success | 81 focused characterization tests passed |
| failure | drift register records unknown/unsafe assumptions as blockers |
| idempotency | no runtime write; repeated inventory/parity query did not mutate state |
| rollback | documentation commit can be reverted; runtime unchanged |
| parity | 23 sync tables: zero business/critical/missing/duplicate/incomplete drift |

Focused tests covered background-job authority, Bot/Web candidate matrix,
deployment surface, Offer/Request source policy, routing/trade forwarding, sync
field/registry policy and Telegram runtime role. The command used the explicit
unit-test environment contract; no production credentials or side effects were
enabled.

## Evidence paths and checksums

See `../evidence-index.md`. Raw command output and production data are not tracked.

## Known gaps / owner decisions

- approve or reject `DR-004` provenance model;
- resolve/deep-review `D-02..D-06`, `D-09` and `D-11`; `D-07`, `D-08` and
  `D-10` are approved;
- finish concrete `behavior_id` coverage for every mutation/side effect;
- approve the dossier and cleanup manifest before `P1-01` execution.

## Rollback trigger / steps / verification

Trigger: a secret, personal row data, false runtime assertion or out-of-scope
change is found in this documentation commit.

Steps: revert only the documentation commit; do not touch hosts/data. Verify
repository status and confirm no external system changed.

## Resulting commits

The documentation/audit commit containing this Task Card is recorded in Git
history and the final human handoff. A later implementation commit is not part of
this read-only Stage execution.

## Human gate receipt

```text
PENDING — owner must review 01-current-finland-architecture.md,
04-surface-policy-matrix.md, 05-feature-parity-contract.md,
06-current-drift-register.md and the proposed decisions.
```
