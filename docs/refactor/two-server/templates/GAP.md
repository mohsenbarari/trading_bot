# Gap — `<GAP_ID>`

- Stage:
- Severity: `<LOW|MEDIUM|HIGH|CRITICAL>`
- State: `<OPEN|MITIGATED|CLOSED|ACCEPTED_RISK>`
- Observed at commit/environment:
- Requirement or invariant:
- Evidence of current behavior:
- User impact in connected / partitioned / reconnecting scenarios:
- Root cause known: `<yes|no>`
- Safe containment:
- Proposed resolution and dependencies:
- Test and rollback required:
- Owner / deadline:
- Closure evidence URI:

`HIGH` و `CRITICAL` بدون fix و evidence بسته نمی‌شوند و waiver تست flaky ندارند.
