# شاهد میانی تأخیر ارسال تلگرام — ۲۰۲۶-۰۸-۲۳

وضعیت: مقایسهٔ کف‌های قفل‌شدهٔ مرحلهٔ ۰ با کف‌های بعد از مرحله‌های ۱ تا ۵.
شاخه: `candidate/telegram-dispatch-latency-v1`
مبنای کد: `44babdc4`
گونهٔ شاهد: `code_derived_midpoint`
محیط زندهٔ آزمایش در این worktree در دسترس نبود؛ صدک مشاهده‌شده ثبت نشد.

این پرونده رفتار را عوض نمی‌کند. فقط همان اعدادی را کنار هم می‌گذارد که از
ثابت‌های تنظیمات و مسیر فعلی فرمان برمی‌آید.

ماژول قفل‌شده: `core/telegram_dispatch_latency_midpoint.py`

## روش

همان سنجهٔ مرحلهٔ ۰ دوباره از کد خوانده شد و با شاهد
`docs/TELEGRAM_DISPATCH_LATENCY_BASELINE_20260823.md` مقایسه شد.

صدک ۵۰/۹۵/۹۹ روی پایگاه آزمایش جمع نشد. حدس زدن صدک ممنوع است. اگر بعداً
گزارشگر سلامت صف روی `staging` اجرا شود، همان اعداد زنده باید جدا ثبت شوند
و این جدول را عوض نکنند.

## مقایسهٔ کف‌ها

| سنجه | قبل از مرحلهٔ ۱ | بعد از مرحلهٔ ۵ | تغییر |
|---|---|---|---|
| فاصلهٔ مشترک مقصد | ۱٫۰۵ ثانیه | ۱٫۰۵ ثانیه | بدون تغییر |
| خواب بی‌کاری خط ناشر | ۱٫۰ ثانیه | ۱٫۰ ثانیه | بدون تغییر؛ فقط تور ایمنی |
| فاصلهٔ دور فرمان بین باتی | ۰٫۵ ثانیه | ۰٫۵ ثانیه | دور بی‌کار همان است |
| اندازهٔ دستهٔ فرمان | ۱ | ۸ | دستهٔ سریال |
| تماس تلگرام برای هر کار کانال | ۳ | ۳ | هنوز دو تماس بین باتی مانده |
| بدترین انتظار مرده بعد از تأیید | ۱٫۰ ثانیه | ۰ | بیدارباش در تأیید |
| خواب بعد از دستهٔ پر | ۰٫۵ ثانیه | ۰ | دور بعد فوری شروع می‌شود |
| دروازهٔ مقصد | مشترک | مشترک | کلید `destination_next` |
| دست‌دادن تازهٔ دروازه | هر تماس | بازیافت اتصال | فقط مسیر ناهمگام |
| نشست احراز برای فرمان | ۲ | ۱ | احراز فقط روی بازخوانی کانال |
| نمایهٔ برداشت فرمان | بدون `sent` | با `sent` | محمول هم‌خوان |
| نگهداشت فرمان پایانی | مسدود | همراه کار | `RESTRICT` سر جایش است |

## چیزی که از روی کد بهتر شده

- انتظار مردهٔ بعد از تأیید، دیگر به خواب یک‌ثانیه‌ای خط ناشر گره نیست.
- دنبالهٔ فرمان در ازدحام به سقف دستهٔ ۸ وابسته است، نه به یک فرمان در هر
  نیم‌ثانیه.
- تماس‌های ناهمگام دروازه اتصال را نگه می‌دارند؛ سه دست‌دادن تازه برای هر
  کار کانال دیگر در مسیر ناهمگام اجباری نیست.
- هر فرمان بین باتی یک نشست پایگاه می‌گشاید، نه دو.
- جدول فرمان با کار پایانی پاک می‌شود و نمایهٔ برداشت با رشد جدول برای
  حالت `sent` هم می‌ماند.

## چیزی که هنوز همان است

- فاصلهٔ کانال ۱٫۰۵ ثانیه و کلید مشترک مقصد دست نخورده‌اند.
- هر کار کانال هنوز سه تماس تلگرام می‌پردازد: فرمان، تأیید، سپس متد کانال.
- بات مرکزی و پنج ناشر هنوز یک پروسه و یک حلقهٔ رویدادند.
- پاسخ دکمه هنوز از صف می‌گذرد.

این‌ها عمداً برای مرحله‌های ۷ تا ۹ مانده‌اند و بدون تأیید مالک شروع
نمی‌شوند.

## چیزی که هنوز عدد زنده ندارد

روی این worktree این‌ها جمع نشد:

- صدک ۵۰/۹۵/۹۹ از ثبت آفر تا دیده شدن پیام
- از تأیید فرمان تا برداشتن کار
- تأخیر پاسخ دکمه از رسید لبه
- شمار `429` به تفکیک بات و مقصد

جای خالی‌شان عمدی است.

## جمع انتظار خواب و نوبت

از روی همان ثابت‌ها، مستقل از شبکه:

- قبل: میانگین تقریبی حدود ۱٫۵ ثانیه؛ بدترین حالت تقریبی حدود ۳ ثانیه
- بعد: انتظار مردهٔ تأیید صفر است؛ فاصلهٔ کانال ۱٫۰۵ ثانیه مانده؛ بدترین
  انتظار دور فرمان برای یک کار تنها همان ۰٫۵ ثانیهٔ دور بی‌کار است

بهبود مشاهده‌شده در کف کد معنادار است: انتظار مرده حذف شد و دنبالهٔ ازدحام
از دستهٔ ۸ پیروی می‌کند. عدد زندهٔ staging هنوز نیست، پس مرحلهٔ ۷ با فرض
بهبود زنده شروع نمی‌شود.
