# Roadmap بخش «رصد بازار» برای مدیریت آفرها

## وضعیت سند

- تاریخ ایجاد: `2026-07-14`
- شاخه اختصاصی: `candidate/admin-market-monitoring`
- مبنای شاخه: `main@04fef2a5`
- وضعیت مبنا هنگام ایجاد شاخه:
  - شاخه `candidate/admin-market-monitoring` مستقیماً از HEAD شاخه `main` ساخته شد.
  - فاصله `main...candidate/admin-market-monitoring` در لحظه ایجاد برابر `0/0` بود.
  - هیچ شاخه candidate قبلی در مبنای این شاخه merge نشده است.
- وضعیت Roadmap: سناریوی محصول WebApp تأیید شده؛ Stage 0 در حال اجرا و `AMM-C01` تا `AMM-C03` و `AMM-C17` نهایی شده‌اند.
- این سند مجوز deploy یا production release نیست.

این Roadmap مسیر کامل ساخت قابلیت «رصد بازار» را از بستن چالش‌ها و قراردادهای محصول تا کدنویسی، تست، مستندسازی و اعتبارسنجی staging مشخص می‌کند. اجرای هر Stage فقط پس از بسته‌شدن معیارهای خروج Stage قبلی مجاز است.

## 1. هدف محصول

مدیر ارشد و مدیر میانی باید در بخشی مستقل از بازار عمومی، همان آفرهای بازار را با همان ترتیب و وضعیت ببینند؛ با این تفاوت که هویت آفر‌دهنده و مسیر مستقیم اقدامات مدیریتی نیز در اختیارشان باشد.

این قابلیت باید به مدیر اجازه دهد بدون جست‌وجوی دستی کاربر:

1. آفر‌دهنده را شناسایی کند.
2. سابقه مرتبط کاربر را ببیند.
3. آفر مشخص را منقضی کند.
4. به کاربر هشدار دهد.
5. محدودیت یا مسدودی اعمال کند.
6. در محدوده اختیار نقش خود، کاربر را غیرفعال یا اخراج کند.

## 2. تصمیم‌های قطعی محصول

موارد زیر ورودی قطعی Roadmap هستند و در Stageهای فنی نباید بدون تأیید مالک محصول تغییر کنند:

- نام دقیق بخش `رصد بازار` است.
- `رصد بازار` بخشی مستقل در پنل مدیریت WebApp است.
- بازار عمومی برای تمام نقش‌ها بدون تغییر باقی می‌ماند.
- هویت آفر‌دهنده نباید به پاسخ عمومی بازار، Realtime عمومی، Web Push عمومی یا کانال عمومی نشت کند.
- نمای مدیریتی همان آفرها، همان ترتیب و همان وضعیت بازار را نمایش می‌دهد.
- این بخش فیلتر بازار ندارد.
- روی هر آفر، فقط هویت آفر‌دهنده واقعی نمایش داده می‌شود.
- actor داخلی آفر به هویت محصولی جداگانه در `رصد بازار` تبدیل نمی‌شود.
- اگر آفر‌دهنده مشتری باشد، خود مشتری آفر‌دهنده است و مالک رابطه فقط به‌عنوان context تکمیلی نمایش داده می‌شود.
- نام آفر‌دهنده مسیر مستقیم به نمای سریع و پروفایل مدیریتی کاربر دارد.
- اقدامات مربوط به خود آفر از اقدامات مربوط به حساب کاربر جدا هستند.
- هشدار یک اقدام مشترک میان WebApp و بات است، نه یک پیام محلی وابسته به پلتفرم.
- هشدار ثبت‌شده از هر مبدأ باید در هر دو پلتفرم قابل مشاهده باشد.
- کاربر باید هشدار را هم در اعلان WebApp و هم در پیام خصوصی بات دریافت کند، مگر اینکه آن سطح برای کاربر واقعاً در دسترس نباشد.
- مبدأ هشدار، مدیر صادرکننده، آفر مرتبط، زمان، علت و نتیجه تحویل باید در سابقه مشترک قابل مشاهده باشد.
- محدودیت، مسدودی و غیرفعال‌سازی باید همان رفتار مشترک دو سرور و دو پلتفرم را حفظ کنند.
- اخراج محصولی به معنی غیرفعال‌سازی عضویت و قطع دسترسی است؛ حذف دائمی داده و سوابق جزو این قابلیت نیست.
- مدیر میانی می‌تواند همه آفرهای نمای مدیریتی را ببیند، اما فقط روی کاربران خارج از نقش‌های مدیریتی اقدام کند.
- اقدام روی مدیران دیگر و اخراج نهایی در اختیار مدیر ارشد است.
- surface بات در گفت‌وگوی خصوصی مدیر و با یک پیام dashboard قابل‌ویرایش و صفحه‌بندی‌شده نمایش داده می‌شود.
- بازه مشاهده فعال بات، تغییرات بازار را به‌صورت خودکار و تجمیع‌شده با هدف تأخیر ۵ تا ۱۰ ثانیه روی همان پیام اعمال می‌کند.
- دکمه `به‌روزرسانی` همیشه وجود دارد و هر interaction مدیر نیز snapshot را تازه می‌کند.
- پس از پایان بازه مشاهده فعال، ویرایش خودکار متوقف و refresh دستی مسیر قطعی باقی می‌ماند.
- برای هر event بازار پیام تازه Telegram ساخته نمی‌شود.
- ارسال‌های حیاتی کانال از refresh قابل‌حذف و کم‌اولویت dashboard مدیریتی اولویت بالاتری دارند.

## 3. سناریوی مرجع WebApp

1. مدیر از پنل مدیریت وارد `رصد بازار` می‌شود.
2. فهرست آفرها بدون فیلتر و با همان ترتیب بازار عمومی نمایش داده می‌شود.
3. بخش عمومی کارت آفر با بازار یکسان است و نوار مدیریتی جداگانه، آفر‌دهنده واقعی و در صورت مشتری‌بودن context رابطه او را نشان می‌دهد.
4. مدیر با انتخاب هویت، نمای سریع کاربر را باز می‌کند.
5. نمای سریع، وضعیت حساب، محدودیت‌های فعال، تعداد آفرهای فعال، آفرهای اخیر و اقدامات مدیریتی قبلی را نمایش می‌دهد.
6. مدیر می‌تواند فقط همان آفر را منقضی کند، بدون اینکه خودکار حساب کاربر محدود شود.
7. مدیر می‌تواند هشدار مرتبط با همان آفر ارسال کند.
8. هشدار یک‌بار ثبت می‌شود و به‌صورت مشترک در WebApp و بات ظاهر می‌شود.
9. در صورت تکرار رفتار، مدیر می‌تواند محدودیت یا مسدودی زمان‌دار اعمال کند و درباره آفرهای فعال فعلی تصمیم بگیرد.
10. در موارد شدید، مدیر ارشد می‌تواند عضویت کاربر را غیرفعال کند؛ آفرهای فعال باید مطابق قرارداد نهایی Stage 0 تعیین تکلیف شوند.
11. همه اقدامات و برگشت آن‌ها در سابقه مدیریتی مشترک باقی می‌مانند.

## 4. محدوده Roadmap

### 4.1 داخل محدوده

- مسیر و ورودی مستقل `رصد بازار` در پنل مدیریت WebApp
- نمای admin-only آفرهای بازار با هویت آفر‌دهنده و context رابطه مشتری
- نمای سریع آفر‌دهنده و اتصال به پروفایل مدیریتی موجود
- منقضی‌کردن مدیریتی یک آفر
- مشاهده آفرهای اخیر کاربر در زمینه مدیریتی
- هشدار مشترک WebApp و بات با سابقه واحد
- اتصال به محدودیت، مسدودی و غیرفعال‌سازی موجود
- رعایت مرجعیت آفر و داده کاربر در دو سرور
- Realtime مدیریتی بدون نشت هویت به Realtime عمومی
- audit، idempotency، retry، observability و history موردنیاز
- تست واحد، integration، cross-server، frontend و E2E
- اعتبارسنجی staging دو سرور پیش از هر تصمیم انتشار
- طراحی و پیاده‌سازی surface بات در Stage مستقل، مطابق سناریوی تأییدشده `AMM-C17`

### 4.2 خارج از محدوده

- تغییر صفحه عمومی بازار یا کارت عمومی آفر
- اضافه‌کردن فیلتر به `رصد بازار`
- امتیازدهی خودکار تخلف یا تشخیص خودکار سوءنیت
- سیستم پرونده‌سازی یا case management مستقل
- نمایش عمومی سابقه هشدار، محدودیت یا اخراج کاربر
- حذف دائمی کاربر یا پاک‌سازی تاریخچه او
- بازطراحی هسته قیمت‌گذاری، معامله یا تسویه
- تغییر قواعد تجاری بازار که ارتباط مستقیمی با اقدام مدیریتی ندارد
- production deploy بدون چرخه جداگانه تأیید انتشار

## 5. واقعیت‌های فعلی کد که روی Roadmap اثر دارند

- `api/routers/offers.py` همین حالا بین پاسخ دارای هویت و پاسخ عمومی بدون هویت تفکیک دارد؛ read عمومی بازار با `include_owner_identity=False` ساخته می‌شود.
- Realtime عمومی ایجاد آفر عمداً `user_id=None` و نام خالی منتشر می‌کند؛ این قرارداد باید محفوظ بماند.
- مدل `Offer` دارای `user_id` برای آفر‌دهنده، `actor_user_id` به‌عنوان داده داخلی، `offer_public_id` برای ارجاع پایدار و `home_server` برای مرجعیت آفر است؛ actor داخلی در UI رصد بازار نمایش داده نمی‌شود.
- endpoint فعلی انقضای آفر فقط مالک آفر را می‌پذیرد؛ مسیر admin action باید مجوز، دلیل و actor مدیریتی جدا داشته باشد.
- سرویس مشترک انقضای آفر از `OfferExpiryCommand`، دلیل `ADMIN_ACTION` و سطح `ADMIN` پشتیبانی اولیه دارد، اما قرارداد کامل اقدام مدیریتی هنوز بسته نشده است.
- آفر باید روی `offer_home_server` منقضی شود؛ WebApp ایران نمی‌تواند آفر foreign-home را محلی mutate کند.
- `frontend/src/views/AdminView.vue` و `frontend/src/components/AdminPanel.vue` seam فعلی ورود به بخش‌های مدیریتی هستند.
- `frontend/src/components/OffersList.vue` و `MarketView.vue` شامل رفتار معامله، فیلترها و state عمومی بازار هستند؛ reuse باید کنترل‌شده باشد تا فیلتر و قابلیت معامله ناخواسته وارد `رصد بازار` نشود.
- `frontend/src/components/UserProfile.vue` و `api/routers/users.py` بخش بزرگی از اقدامات محدودیت، مسدودی و وضعیت حساب را در اختیار دارند.
- `core/admin_authority.py` برای داده‌های مدیریتی مشترک، ایران را single writer می‌داند؛ بات foreign برای هشدار یا تغییر کاربر به مسیر forwarding معتبر نیاز دارد.
- جدول `notifications` داده مشترک و sync‌شونده است، اما خود اعلان محصولی با سابقه مدیریتی کامل هشدار یکسان نیست.
- Telegram فقط روی foreign اجرا می‌شود و Web Push/WebApp realtime فقط روی سطح ایران اجرا می‌شوند؛ ثبت هشدار نباید به اجرای مستقیم side effect در سرور اشتباه منجر شود.
- user actions فعلی audit دارند، اما مدل واحدی برای تاریخچه هشدار مرتبط با آفر و وضعیت تحویل دو پلتفرم وجود ندارد.

## 6. اصول معماری غیرقابل تغییر

- پاسخ عمومی و پاسخ مدیریتی آفر دو قرارداد مجزا هستند.
- client-side hide/show به‌تنهایی مجوز محسوب نمی‌شود؛ مجوز admin باید در backend enforce شود.
- هویت نباید ابتدا در payload عمومی ارسال و سپس در UI پنهان شود.
- هر mutation آفر باید روی home server همان آفر اجرا شود.
- هر mutation مشترک کاربر یا هشدار باید یک مرجع نوشتن مشخص، retry امن و نتیجه idempotent داشته باشد.
- یک هشدار محصولی فقط یک identity و یک سابقه canonical دارد؛ WebApp و بات دو projection/delivery از همان هشدار هستند.
- شکست یکی از deliveryها نباید رکورد اصلی هشدار را از بین ببرد یا باعث ثبت هشدار دوم شود.
- retry نباید هشدار، نوتیفیکیشن یا پیام تلگرام تکراری ایجاد کند.
- هر اقدام حساس باید actor، target، offer، reason، source surface، timestamp و outcome قابل audit داشته باشد.
- هیچ Stage نباید برای ساده‌سازی، مرجعیت offer یا user را دور بزند.

## 7. فهرست چالش‌های مرحله بعد

راهنمای وضعیت:

- `OPEN`: هنوز تحلیل و تصمیم نهایی نشده است.
- `DECIDED`: قرارداد تصمیم گرفته شده ولی هنوز پیاده‌سازی نشده است.
- `IMPLEMENTED`: پیاده‌سازی و تست شده است.
- `VERIFIED`: در staging دو سرور تأیید شده است.

| شناسه | اولویت | وضعیت | چالش | خروجی لازم برای بسته‌شدن |
| --- | --- | --- | --- | --- |
| `AMM-C01` | P0 | DECIDED | تعریف دقیق «همان آفرهای بازار» برای active و terminal rows بدون فیلتر | قرارداد feed، ترتیب، دسترسی به feed و رفتار rowهای معامله‌شده/منقضی |
| `AMM-C02` | P0 | DECIDED | تعریف هویت آفر‌دهنده در سناریوهای کاربر عادی و مشتری، بدون نمایش actor داخلی | ماتریس identity آفر‌دهنده، context مشتری و label قطعی نمای سریع |
| `AMM-C03` | P0 | DECIDED | جلوگیری قطعی از نشت هویت به API و Realtime عمومی | read model مستقل admin-only، Realtime عمومی بدون هویت و refetch مدیریتی با تست منفی |
| `AMM-C04` | P0 | OPEN | ماتریس مجوز مدیر میانی و مدیر ارشد برای دیدن و اقدام | جدول permission سمت backend و UI با حالت disabled/denied |
| `AMM-C05` | P0 | OPEN | انقضای مدیریتی آفرهای Iran-home و foreign-home | command forwarding، idempotency، lock و side-effect contract |
| `AMM-C06` | P0 | OPEN | تعریف مدل هشدار مشترک و تفاوت آن با notification ساده | schema/lifecycle/history/dedupe/retention contract |
| `AMM-C07` | P0 | OPEN | ثبت هشدار از بات foreign با مرجعیت داده مشترک در ایران | مسیر command forwarding و پاسخ قطعی به بات |
| `AMM-C08` | P0 | OPEN | تحویل هم‌زمان هشدار در WebApp و بات با قطعی موقت یکی از سرورها | SLA، pending state، retry، delivery result و عدم duplication |
| `AMM-C09` | P0 | OPEN | هم‌راستایی محدودیت، مسدودی و غیرفعال‌سازی با اقدام آفرمحور | reuse contract بدون ایجاد mutation موازی یا policy متفاوت |
| `AMM-C10` | P0 | OPEN | تعریف دقیق اخراج محصولی و پیامد آن بر آفرهای فعال، نشست و پیام‌رسان | قرارداد inactive/expel و rollback/reactivation |
| `AMM-C11` | P1 | OPEN | reuse کارت بازار بدون ورود فیلتر، composer یا دکمه معامله | component boundary و UI contract مستقل `رصد بازار` |
| `AMM-C12` | P1 | OPEN | نمایش سابقه آفر و اقدام بدون کوئری سنگین یا نشت داده | query scope، pagination و retention policy |
| `AMM-C13` | P1 | OPEN | Realtime مدیریتی در زمان create/update/expire/trade و sync apply | event contract امن و refresh fallback |
| `AMM-C14` | P1 | OPEN | رفتار همزمان دو مدیر روی یک آفر یا کاربر | conflict، optimistic/row lock و idempotency rules |
| `AMM-C15` | P1 | OPEN | وضعیت هشدار پس از حذف/غیرفعال‌شدن کاربر یا پاک‌شدن Telegram binding | retention و delivery fallback contract |
| `AMM-C16` | P1 | OPEN | observability بدون ثبت هویت و شماره موبایل در logهای عمومی | safe logging، metrics و alert contract |
| `AMM-C17` | P1 | DECIDED | سناریوی دقیق UI بات برای رصد و اقدام روی آفر | dashboard خصوصی صفحه‌بندی‌شده، refresh ترکیبی و قرارداد حفاظت از ظرفیت ارسال کانال |
| `AMM-C18` | P0 | OPEN | ماتریس تست دو سرور و دو surface پیش از staging | automated acceptance matrix و stop conditions |

هیچ migration یا کدنویسی feature قبل از بسته‌شدن `AMM-C01` تا `AMM-C10` و `AMM-C18` شروع نمی‌شود.

## 8. ترتیب و وابستگی Stageها

```text
Stage 0  Challenge closure and contracts
   |
   +--> Stage 1  Shared moderation domain and persistence
   |       |
   |       +--> Stage 2  Admin-only market read model/API
   |       |       |
   |       |       +--> Stage 3  WebApp رصد بازار shell and cards
   |       |               |
   |       |               +--> Stage 4  Quick user context and history
   |       |
   |       +--> Stage 5  Admin offer actions and authority routing
   |       |
   |       +--> Stage 6  Shared warning delivery
   |               |
   |               +--> Stage 7  Restrict/block/expel integration
   |
   +--> Stage 8  Bot surface after product approval
           |
           +--> Stage 9  Full matrix, observability, staging acceptance
```

Stageهای 2 تا 4 می‌توانند پس از تثبیت قرارداد Stage 1 به‌صورت sliceهای کوچک اجرا شوند، اما Stageهای 5 تا 8 بدون بستن authority و idempotency مجاز نیستند.

## 9. Stage 0 - رفع چالش‌ها و بستن قراردادها

### هدف

تبدیل همه P0های باز به تصمیم‌های قابل تست، پیش از هر migration یا feature code.

### خروجی‌ها

- مستند تحلیل و تصمیم هر challenge
- ماتریس identity آفر‌دهنده و context رابطه مشتری
- ماتریس permission مدیر میانی/ارشد
- قرارداد feed بدون فیلتر
- قرارداد admin offer expiry برای هر دو home server
- قرارداد canonical warning و delivery دو پلتفرم
- قرارداد restrict/block/expel و پیامد آفرهای فعال
- ماتریس خطا، retry، outage و همزمانی
- acceptance matrix اولیه

### معیار خروج

- `AMM-C01` تا `AMM-C10` و `AMM-C18` وضعیت `DECIDED` داشته باشند.
- برای هر تصمیم، سناریوی مثبت، منفی، retry و outage ثبت شده باشد.
- هیچ تصمیمی با سیاست عمومی بازار یا مرجعیت دو سرور تعارض نداشته باشد.
- مالک محصول تصمیم‌هایی را که رفتار کاربر را تغییر می‌دهند تأیید کرده باشد.

## 10. Stage 1 - دامنه و تاریخچه مشترک اقدامات مدیریتی

### هدف

ایجاد یک منبع حقیقت برای هشدار و اقدام مدیریتی مرتبط با آفر، مستقل از مبدأ WebApp یا بات.

### محدوده

- مدل canonical اقدام/هشدار مدیریتی
- identity پایدار و dedupe key
- actor، target، offer public identity، reason، source و outcome
- lifecycle و delivery state برای WebApp و Telegram
- ثبت sync policy، field policy و retention
- migration و backfill فقط در صورت نیاز واقعی

### تست‌های اجباری

- duplicate submit و retry
- actor/target/offer validation
- sync payload و apply
- sensitive-field redaction
- history ordering و retention

### معیار خروج

- یک هشدار از هر مبدأ فقط یک رکورد canonical تولید کند.
- retry یا sync replay رکورد یا delivery تکراری نسازد.
- peer بتواند همان history را بدون تناقض نمایش دهد.

## 11. Stage 2 - Read model و API اختصاصی رصد بازار

### هدف

ارائه feed مدیریتی دارای هویت، بدون تغییر قرارداد feed عمومی.

### محدوده

- endpoint یا projection admin-only برای آفرها
- هویت آفر‌دهنده و context رابطه مشتری طبق قرارداد Stage 0
- ترتیب و pagination طبق `AMM-C01`
- permission سمت backend
- پاسخ امن برای deleted/inactive/missing relations
- تست عدم نشت هویت در endpointها و eventهای عمومی

### معیار خروج

- مدیر مجاز feed را با هویت کامل موردنیاز دریافت کند.
- کاربر عادی، حسابدار یا درخواست بدون نقش مجاز پاسخ admin را دریافت نکند.
- payload عمومی بازار، public link و Realtime عمومی قبل و بعد از Stage یکسان بمانند.

## 12. Stage 3 - صفحه WebApp «رصد بازار»

### هدف

اضافه‌کردن ورودی مستقل در پنل مدیریت و نمایش feed مدیریتی بدون فیلتر.

### محدوده

- route و section مستقل با عنوان دقیق `رصد بازار`
- دسترسی مدیر میانی و مدیر ارشد
- همان ترتیب و وضعیت بازار
- عدم نمایش filter chips، composer و کنترل معامله
- نوار هویت آفر‌دهنده و context رابطه مشتری
- loading، empty، error و reconnect state
- navigation و back-button هماهنگ با AdminView

### معیار خروج

- صفحه روی mobile/PWA و desktop پایدار باشد.
- هیچ فیلتر بازار در این صفحه دیده نشود.
- کارت عمومی و نوار مدیریتی از نظر دسترسی و styling مرز روشن داشته باشند.
- non-admin از route و داده admin محروم باشد.

## 13. Stage 4 - نمای سریع آفر‌دهنده و سابقه مرتبط

### هدف

حذف جست‌وجوی دستی کاربر از فرایند مدیریت آفر.

### محدوده

- بازکردن نمای سریع از روی هویت کارت
- وضعیت حساب و محدودیت‌های فعال
- تعداد آفرهای فعال
- آفرهای اخیر طبق scope نهایی Stage 0
- هشدارها و اقدامات قبلی
- مسیر مستقیم به پروفایل مدیریتی موجود

### معیار خروج

- مدیر از یک آفر با حداکثر یک interaction به context کاربر برسد.
- داده نمای سریع با پروفایل مدیریتی تناقض نداشته باشد.
- مدیر میانی برای target خارج از اختیار فقط رفتار مجاز تعریف‌شده را ببیند.

## 14. Stage 5 - اقدامات مدیریتی روی آفر

### هدف

منقضی‌کردن آفر مشخص با دلیل مدیریتی، بدون اعمال خودکار محدودیت حساب.

### محدوده

- admin expire command مجزا از self-expire کاربر
- route به `offer_home_server`
- ثبت actor admin و reason `admin_action`
- idempotency و lock برای اقدام همزمان
- Telegram channel state و WebApp realtime پس از commit authoritative
- notification نتیجه برای آفر‌دهنده

### معیار خروج

- آفر Iran-home و foreign-home از WebApp مدیر به شکل صحیح منقضی شوند.
- peer و کانال به وضعیت نهایی همگرا شوند.
- محدودیت self-expire کاربر به admin action تحمیل نشود مگر قرارداد Stage 0 خلاف آن را تعیین کند.
- اقدام دوم روی آفر غیرفعال، نتیجه idempotent/قابل فهم بدهد و mutation جدید نسازد.

## 15. Stage 6 - هشدار مشترک WebApp و بات

### هدف

ثبت یک هشدار canonical و تحویل آن در هر دو پلتفرم، مستقل از مبدأ اقدام.

### محدوده

- composer یا reason picker WebApp
- اتصال هشدار به آفر و target
- اعلان داخل WebApp
- پیام خصوصی Telegram از foreign
- delivery status، retry و dedupe
- نمایش history مشترک در WebApp و بات
- ثبت source surface بدون دوپاره‌کردن رکورد

### معیار خروج

- هشدار WebApp در بات و WebApp دیده و تحویل شود.
- هشدار بات در WebApp و بات دیده و تحویل شود.
- قطعی موقت یک peer به از دست‌رفتن هشدار یا duplication منجر نشود.
- مدیر بتواند وضعیت pending/delivered/failed هر surface را ببیند.

## 16. Stage 7 - اتصال محدودیت، مسدودی و اخراج

### هدف

استفاده از policyهای مشترک موجود از داخل context آفر، بدون ساخت مسیر مدیریتی موازی.

### محدوده

- بازکردن actionهای موجود از کارت/نمای سریع
- ثبت offer reference و reason در audit/history
- تعیین تکلیف فقط همان آفر، همه آفرهای فعال یا آفرهای آینده طبق انتخاب تأییدشده
- permission مدیر میانی/ارشد
- اخراج reversible بر مبنای account inactive
- reactivation و رفع محدودیت با history کامل

### معیار خروج

- نتیجه اقدام از هر سرور در هر دو پلتفرم یکسان باشد.
- middle manager نتواند target مدیریتی یا action خارج از اختیار را mutate کند.
- اخراج، سابقه آفر و اقدام را حذف نکند.
- رفع محدودیت یا فعال‌سازی مجدد، سابقه قبلی را بازنویسی نکند.

## 17. Stage 8 - surface بات

### وضعیت تصمیم محصول

سناریوی UX بات در تاریخ `2026-07-15` تأیید و challenge `AMM-C17` در وضعیت `DECIDED` قرار گرفت. هشدار و history همچنان میان بات و WebApp مشترک هستند.

### هدف

اضافه‌کردن مسیر بات برای رصد و اقدام، با reuse کامل commandهای Stageهای قبلی.

### قرارداد UX تأییدشده

- ورود admin-only به رصد آفرها
- نمایش در گفت‌وگوی خصوصی مدیر، نه کانال عمومی
- یک پیام dashboard قابل‌ویرایش به‌جای ارسال پیام تازه در هر navigation یا event
- pagination به‌دلیل تعداد آفرها و سقف متن پیام Telegram
- دکمه‌های `قبلی`، `بعدی`، `به‌روزرسانی`، مشاهده جزئیات و اقدام‌های مجاز
- نمایش آفر‌دهنده و context رابطه مشتری
- نمای خلاصه کاربر
- هشدار، انقضای آفر و ورود به اقدام‌های مجاز
- forwarding همه shared writes به مرجع صحیح
- شروع یک بازه مشاهده فعال با ورود مدیر به صفحه و تمدید آن با هر interaction
- تجمیع رخدادهای پشت‌سرهم و ویرایش همان پیام با هدف فاصله ۵ تا ۱۰ ثانیه، نه یک edit برای هر event
- توقف refresh خودکار پس از پایان بازه مشاهده فعال و باقی‌ماندن refresh دستی
- refresh snapshot در هر callback و پاسخ سریع `answerCallbackQuery`

### قرارداد حفاظت از ظرفیت Telegram

- dashboard مدیریتی هیچ پیام یا editای در chat کانال عمومی ایجاد نمی‌کند.
- per-chat کانال و چت خصوصی مدیر از هم جدا هستند، اما ظرفیت کلی همان bot token مشترک است.
- صف ارسال کانال اولویت بالاتر دارد؛ refresh dashboard کم‌اولویت، تجمیع‌پذیر و قابل جایگزینی است.
- در هر لحظه برای هر dashboard حداکثر یک refresh pending نگه داشته می‌شود.
- پاسخ 429 طبق مقدار `retry_after` retry می‌شود و با loop سریع تکرار نمی‌شود.
- paid broadcast برای refresh dashboard استفاده نمی‌شود.

مراجع رسمی Telegram:

- [Bot FAQ - محدودیت ارسال](https://core.telegram.org/bots/faq#my-bot-is-hitting-limits-how-do-i-avoid-this)
- [Bot API - ویرایش متن پیام](https://core.telegram.org/bots/api#editmessagetext)
- [Bot API - پاسخ callback](https://core.telegram.org/bots/api#answercallbackquery)
- [Bot API - retry_after](https://core.telegram.org/bots/api#responseparameters)

### معیار خروج

- بات هیچ mutation محلی غیرمرجع انجام ندهد.
- نتیجه همان command در WebApp و history مشترک دیده شود.
- UX نهایی با سناریوی تأییدشده مالک محصول منطبق باشد.
- مدیر در بازه مشاهده فعال، تغییرات تجمیع‌شده را بدون refresh دستی و بدون پیام تازه مشاهده کند.
- دکمه refresh در تمام صفحات dashboard در دسترس باشد.
- پایان بازه مشاهده فعال، ویرایش خودکار را متوقف کند.
- burst رخدادهای بازار به یک edit تجمیع شود و صف کانال را عقب نیندازد.
- 429 با `retry_after` مدیریت شود و موجب loop یا از دست‌رفتن ارسال حیاتی کانال نشود.

## 18. Stage 9 - سخت‌سازی، ماتریس کامل و staging

### هدف

اثبات اینکه قابلیت در حالت عادی، قطعی، retry، همزمانی و sync lag رفتار درست دارد.

### تست‌های الزامی

- unit و permission tests
- API integration tests
- frontend component/view tests
- realtime and reconnect tests
- offer authority forwarding tests برای هر دو home server
- warning origin/delivery matrix برای WebApp و بات
- sync replay و dedupe tests
- two-admin concurrency tests
- inactive/deleted/missing-user edge cases
- E2E WebApp admin scenarios
- Bot/WebApp two-server acceptance matrix
- تست منفی عدم نشت هویت به بازار عمومی

### معیار خروج

- همه سناریوهای P0 سبز باشند.
- sync backlog و delivery backlog پس از تست به صفر یا وضعیت توضیح‌داده‌شده برسند.
- staging روی هر دو سرور با داده و Telegram/WebApp staging معتبر تأیید شود.
- مالک محصول سناریوهای دستی را تأیید کند.
- هیچ production action انجام نشده باشد.

## 19. ماتریس پذیرش حداقلی

| سناریو | نتیجه الزامی |
| --- | --- |
| کاربر عادی بازار را باز می‌کند | هیچ هویت آفر‌دهنده‌ای دریافت یا مشاهده نمی‌کند |
| مدیر میانی `رصد بازار` را باز می‌کند | همان feed بدون فیلتر را همراه هویت می‌بیند |
| non-admin مسیر admin را مستقیم فراخوانی می‌کند | backend درخواست را رد می‌کند |
| آفر متعلق به مشتری سطح ۱ است | خود مشتری آفر‌دهنده و مالک رابطه فقط context تکمیلی نمایش داده می‌شود |
| مدیر آفر Iran-home را منقضی می‌کند | ایران authoritative mutate می‌کند و foreign/channel همگرا می‌شوند |
| مدیر آفر foreign-home را از WebApp منقضی می‌کند | command به foreign route می‌شود و ایران محلی mutate نمی‌کند |
| مدیر از WebApp هشدار می‌دهد | یک رکورد، اعلان WebApp و پیام بات ایجاد می‌شود |
| مدیر از بات هشدار می‌دهد | همان قرارداد یک رکورد و دو delivery اجرا می‌شود |
| مدیر در بات وارد `رصد بازار` می‌شود | یک dashboard خصوصی و صفحه‌بندی‌شده با دکمه refresh می‌بیند |
| آفرها در بازه مشاهده فعال بات تغییر می‌کنند | همان پیام با refresh تجمیع‌شده به‌روز می‌شود و پیام تازه ساخته نمی‌شود |
| بازه مشاهده فعال بات پایان می‌یابد | refresh خودکار متوقف و refresh دستی فعال می‌ماند |
| چند رخداد بازار پشت‌سرهم می‌رسد | فقط آخرین snapshot لازم edit می‌شود و صف ارسال کانال اولویت دارد |
| delivery بات موقتاً ناموفق است | هشدار canonical و WebApp باقی می‌مانند و Telegram retry می‌شود |
| درخواست هشدار retry می‌شود | هشدار و پیام تکراری ساخته نمی‌شود |
| دو مدیر همزمان روی یک آفر اقدام می‌کنند | فقط یک mutation معتبر و دو نتیجه قابل فهم ثبت می‌شود |
| مدیر میانی target مدیریتی را تغییر می‌دهد | backend اقدام را رد می‌کند |
| مدیر ارشد کاربر را اخراج می‌کند | حساب غیرفعال، آفرها تعیین تکلیف و سوابق حفظ می‌شوند |
| sync apply روی peer انجام می‌شود | Realtime محلی admin به‌روز می‌شود و echo sync ساخته نمی‌شود |

## 20. دروازه مشترک بسته‌شدن هر Stage

هر Stage فقط وقتی بسته است که:

1. challengeهای وابسته آن `DECIDED` یا بالاتر باشند.
2. تغییرات از محدوده Stage فراتر نرفته باشند.
3. تست‌های مثبت، منفی، permission، retry و failure مرتبط سبز باشند.
4. `git diff --check` سبز باشد.
5. مستند Stage با تصمیم، فایل‌های تغییرکرده، تست‌ها و ریسک باقی‌مانده به‌روز شده باشد.
6. branch پیش از edit، test و commit دوباره کنترل شده باشد.
7. commit فقط روی `candidate/admin-market-monitoring` ایجاد شده باشد.
8. تغییرات نامرتبط از main، candidate یا worktreeهای دیگر وارد commit نشده باشند.

## 21. سیاست شاخه و ادغام

- تمام مستندات، migrationها، کد، تست‌ها و اصلاحات این قابلیت فقط روی `candidate/admin-market-monitoring` انجام می‌شوند.
- `main` برای این قابلیت مستقیم تغییر نمی‌کند و فقط مبنای شاخه است.
- این شاخه مستقیماً روی `main@04fef2a5` بازپایه‌گذاری شده و هیچ شاخه candidate دیگری مبنای آن نیست.
- اگر `main` در طول اجرا جلو رفت، ادغام آن فقط پس از بررسی divergence، diff، merge-tree و تست Stage جاری انجام می‌شود.
- تغییرات local و uncommitted سایر worktreeها متعلق به این قابلیت نیستند و نباید وارد شاخه شوند.
- هر Stage commit مستقل و قابل مرور دارد.
- push، staging deploy و production deploy هرکدام gate و گزارش جدا دارند؛ ساخت Roadmap به‌تنهایی مجوز هیچ‌کدام نیست.

## 22. خروجی مرحله بعد

مرحله بعد فقط Stage 0 است. خروجی آن باید یک مستند challenge-resolution باشد که `AMM-C01` تا `AMM-C10` و `AMM-C18` را به‌ترتیب اولویت تحلیل کند، گزینه‌ها و trade-offها را نشان دهد و برای هر مورد تصمیم قابل تست ثبت کند. تا قبل از تأیید این خروجی، feature code و migration شروع نمی‌شود.

## 23. معیار بسته‌شدن کل Roadmap

- صفحه `رصد بازار` مطابق تصمیم‌های قطعی و بدون فیلتر در WebApp آماده باشد.
- بازار عمومی هیچ تغییر هویتی یا دسترسی ویژه‌ای نداشته باشد.
- identity آفر‌دهنده و context مشتری در تمام سناریوهای مجاز صحیح باشد.
- اقدامات آفرمحور روی home server صحیح اجرا شوند.
- هشدار از هر مبدأ یک رکورد مشترک و دو delivery قابل پیگیری داشته باشد.
- محدودیت، مسدودی و اخراج در هر دو سرور و پلتفرم همگرا باشند.
- سطح دسترسی مدیر میانی و مدیر ارشد در backend و UI enforce شده باشد.
- history و audit کامل و بدون داده حساس در logهای عمومی باشد.
- ماتریس خودکار و دستی staging کامل عبور کرده باشد.
- مستندات اجرا، rollback و محدودیت‌های باقی‌مانده تکمیل شده باشند.
- انتشار production فقط پس از درخواست و تأیید مستقل مالک محصول بررسی شود.
