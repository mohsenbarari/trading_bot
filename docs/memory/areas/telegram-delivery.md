# Telegram Delivery

- 2026-08-22 | Queue-v1 pre-auth replies from `CallbackQuery` retain that route; never pass bot-authored `callback.message` to the message adapter. Registration handoff accepts the originating event, with actor/chat static and runtime guards.
- 2026-08-21 | Sync may promote only Iran's exact legacy `primary/pending/v1` Telegram placeholder from a newer canonical foreign-owned `publisher_1..5` state. Bind authority/identity, forbid message injection, require rowcount one and a transaction marker. Repair uses fresh DB-sequenced `ChangeLog`, never direct row edits or consumed sequences.
- 2026-08-21 | Staging has five distinct Publishers; all six identities passed provider/channel checks and shared opt-in is false. Any credential/channel collision blocks production. APIs stay token-free producers; foreign assigns WebApp offers. Production deployment is separate.
- 2026-08-18 | Offer numeric IDs are local; suggestions retain `offer_public_id` and rebind to the source mirror. Keep bounded overtime polling/countdown. Terminal ratios stay bottom-left/LTR. Reachability uses `telegram_id`, then username/`tg://user?id=`; stale unlinked usernames remain unknown.
- 2026-08-17 | Telegram Mini App and `/api/auth/webapp-login` are retired; never revive or provision them. WebApp auth is OTP-first.
- 2026-08-19 | Staging Queue-v1 executes only on the bot host; APIs produce only. OTP uses encrypted Redis, ACK+DELETE and poison quarantine. Primary/outbox/trade poll at 200ms, Publishers at 500ms; preserve editor cadence, ordering/retries and 1.05s destination limiting. Empty callback ACK may precede outcome display only as a receipt; success stays post-commit. Two-tap confirmation is one atomic Redis toggle. Health counts outstanding work; only audited reconciliation repairs.
- 2026-08-16 | Overtime writes/reconciliation/sweeps run only on offer/request home; mirrors reject writes. Bot-home presents in Telegram and WebApp-home in WebApp. Interaction remains at origin; cancellation is signed, fail-closed, idempotent and forwarded home. Local receipts never bump ledger version.
- 2026-08-12 | The 500-offer matrix uses 60/40 Bot/WebApp origins, randomized 0.8–4s ingress, fake private transport, bounded lifecycles, redacted audits and unconditional cleanup. Unknown-client quarantine proves execution, not delivery.
- 2026-08-11 | Queue-v1 uses central ingress, durable B2B receipts and sticky first-publish ownership across five identities; callbacks stay receiver-local. Scans skip non-final jobs. Only fenced pre-dispatch serialization/deadlock retries; other DB failures close. Freshness changes are fenced requeues.
- 2026-08-11 | Interaction probes require authenticated private chat and positive message identity. Resume clears shared destination cadence; only preflight-approved lanes block.
