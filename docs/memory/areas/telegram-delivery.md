# Telegram Delivery

- 2026-08-21 | Sync may promote only an exact Iran legacy `primary/pending/v1` Telegram placeholder from a newer canonical foreign-owned `publisher_1..5` state. Bind source/owner/surface/public identity, forbid message injection, require unit rowcount and a transaction-local DB marker. Historical repair uses a fresh DB-sequenced `ChangeLog` through the normal worker—never consumed sequences or direct business-row edits.
- 2026-08-21 | Staging has five distinct Publishers; all six identities passed provider/channel readback and shared opt-in is false. Credential/channel collision blocks production. Token-free producer APIs retain multi-publisher+B2B routing; the foreign feeder assigns WebApp offers. Production deployment is separate.
- 2026-08-18 | Numeric offer IDs are server-local; suggestions retain `offer_public_id` and rebind to the source mirror. Keep bounded polling for `overtime_delivering` and local countdown decay. Terminal ratios stay bottom-left/LTR. Reachability uses `telegram_id`, with optional username and `tg://user?id=` fallback; stale unlinked usernames remain unknown.
- 2026-08-17 | Telegram Mini App and `/api/auth/webapp-login` are retired; never revive, migrate, support, or provision API credentials for them. WebApp auth is OTP-first.
- 2026-08-19 | Staging Queue-v1 executes only on the bot host; APIs only produce. OTP uses encrypted Redis, ACK+DELETE, and poison quarantine. Primary polls at 200ms, Publishers at 500ms, outbox/trade feeders at 200ms; preserve editor cadence, sticky ownership, ordering, retries, and 1.05s destination limiting. Realtime/trade receipts require bounded `change_log` drain after wake. Health counts outstanding work; SMS blocks independently; only the audited reconciler repairs.
- 2026-08-16 | Overtime writes/reconciliation/sweeps run only on offer/request home; mirrors reject writes. Present Bot-home in Telegram and WebApp-home in WebApp. Interaction stays at origin; cancellation is signed, fail-closed, idempotent, and forwarded home. Local Telegram receipts never bump ledger version.
- 2026-08-12 | The 500-offer matrix uses 60/40 Bot/WebApp origin, randomized 0.8–4s ingress, fake private transport, bounded lifecycles, redacted audits, and unconditional cleanup. Unknown-client quarantine proves execution, not delivery.
- 2026-08-11 | Queue-v1 uses central ingress, durable B2B receipts, and sticky first-publish ownership across five identities; callbacks stay receiver-local. Scans skip non-final jobs. Only fenced pre-dispatch serialization/deadlock aborts retry; other DB failures close. Freshness changes are expected fenced requeues.
- 2026-08-11 | Interaction probes require authenticated private chat and positive message identity. Resume clears shared destination cadence; only preflight-approved lanes block.
