# Telegram Delivery

- 2026-08-21 | Staging has a distinct five-Publisher fleet; all six identities passed provider channel/permission readback and shared opt-in is false in both envs. Credential/channel collision or shared opt-in blocks production cutover. Producer-only API/sync stay token-free and worker-disabled but retain multi-publisher+B2B routing so WebApp offers remain unassigned until the foreign feeder selects a lane. Production deploy remains separate.
- 2026-08-18 | Numeric offer IDs are server-local; suggestions rebind to the source mirror and retain `offer_public_id`. `overtime_delivering` stays queued with bounded fast polling; countdown uses presentation/deadline and decays locally. Channel terminal ratios stay bottom-left/LTR. Reachability uses `telegram_id`, never `has_bot_access`; username is optional with `tg://user?id=` fallback, while stale unlinked usernames remain unknown.
- 2026-08-17 | Telegram Mini App and `/api/auth/webapp-login` are retired. Never migrate, revive, provision API bot credentials for, or support them; WebApp authentication is OTP-first.
- 2026-08-19 | Staging Queue-v1 executes only on the bot host; API/WebApp only produce. OTP uses encrypted Redis with terminal ACK+DELETE and poison quarantine; omit optional `None`. Primary/M0 poll at 200ms, publishers at 500ms; B2B idle does not double. Outbox/trade feeders remain 200ms; editor cadence, sticky ownership, retries, ordering, and the 1.05s destination limiter stay fixed. Offer text yields `OFFER_VALIDATION_RESPONSE`; publishers run only offer actions/owner callbacks. Realtime/trade receipts require bounded `change_log` drain after Redis wake plus the partial unsynced index. Health counts outstanding work; SMS blocks independently; only the audited reconciler repairs.
- 2026-08-16 | Overtime writes, reconciliation, and sweepers run only on offer/request home; reject mirror writes. Bot-home presents via Telegram and WebApp-home via WebApp. Interaction stays on its source; cancellation is signed, fail-closed, idempotent, and forwarded home. Local Telegram receipts never bump/sync ledger version. Preference saves show only the result; do not restore the cross-surface warning.
- 2026-08-12 | The 500-offer matrix uses 60/40 Bot/WebApp origin, randomized 0.8–4s ingress, fake private transport, bounded lifecycles, redacted audits, and unconditional cleanup. Unknown-client quarantine proves execution, not delivery.
- 2026-08-11 | Queue-v1 uses central ingress, durable B2B receipts, and sticky first-publish ownership across five identities; callbacks remain receiver-local. Scans skip non-final jobs. Only pre-dispatch serialization/deadlock aborts retry under fenced cadence; other DB failures close. Freshness changes are expected fenced requeues.
- 2026-08-11 | Interaction probes require authenticated private chat and positive message identity. Resume clears shared destination cadence and only preflight-approved lane blocks.
