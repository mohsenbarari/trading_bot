# Telegram Delivery

- 2026-08-23 | One Queue Executor only. Default remains `TELEGRAM_BOT_SPLIT_ENABLED=false` and role `all`. Split `primary` polls the central bot, local-acks, and must not acquire the global owner or run OTP. Split `executor` (retired name `publishers`) polls the five worker bots, owns every queue lane plus OTP, and takes the one global owner before any publisher poll or provider call. Status is READY FOR STAGING INTEGRATION REVIEW; no live percentiles.
- 2026-08-23 | Local B2B ack is the only path: lease-fenced, emits wakeup. Never reintroduce Telegram bot-to-bot, a lane-liveness table, or a local-ack flag. Stage 8 answers callbacks on the edge; the worker must not send a second `answerCallbackQuery`. Freshness guards stay; only the duplicate unlocked Offer/state read inside the last validate is gone. Do not shrink Settings `db_pool_size` 15.
- 2026-08-23 | Latency floors stay code-derived: ack wakeup, shared HTTP client, serial batch 8, claim index includes `sent`, destination interval 1.05s. Ignore co-located staging by exact project/service/role; unknown or second executors block. Pre-mutation failure needs no recovery.
- 2026-08-22 | Staging roles fail closed: `trading_bot_staging` is foreign/bot and `_iran` Iran/API. Repair preserves identity/authority.
- 2026-08-21 | Sync may promote only Iran's `primary/pending/v1` placeholder from newer foreign `publisher_1..5`. Bind authority/identity, forbid message injection, require rowcount one. APIs are token-free producers.
- 2026-08-19 | Queue-v1 runs on bot; APIs produce. OTP uses encrypted Redis, ACK+DELETE and quarantine. Preserve retries and 1.05s/destination. ACK is receipt-only; success follows commit.
- 2026-08-11 | Queue-v1 central ingress uses sticky five-publisher ownership. Callbacks remain receiver-local; scans skip final jobs; freshness changes use fenced requeues.
