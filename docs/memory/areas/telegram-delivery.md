# Telegram Delivery

- 2026-08-18 | Cross-server numeric offer IDs are local: lot suggestions rebind to the source mirror and keep stable `offer_public_id`. `overtime_delivering` stays queued under bounded fast polling; countdown starts from presentation/deadline and remaining-only snapshots decay locally. Channel terminal footers stay bottom-left via an LTR numeric ratio. Profile reachability uses `telegram_id`, never `has_bot_access`; username is optional (`tg://user?id=` fallback), while unlinked stale usernames remain unrecognized.
- 2026-08-17 | Telegram Mini App and `/api/auth/webapp-login` are retired product surfaces. Do not migrate, revive, provision API bot credentials for, or treat them as supported; current WebApp authentication is OTP-first.
- 2026-08-19 | Staging Queue-v1 runs only on the bot host; API/WebApp are producer-only. OTP uses an encrypted Redis stream with terminal ACK+DELETE and poison quarantine; the gateway omits optional `None`. Latency tuning changes idle detection only: two primary general slots and M0-reserved poll at 200ms, publishers at 500ms, and B2B idle cycles no longer double their interval. Outbox/trade feeders stay 200ms; editor cadence, sticky ownership, retries, ordering, and the 1.05s destination limiter are immutable. Offer text always yields `OFFER_VALIDATION_RESPONSE`; publishers execute only offer actions/owner callbacks. Cross-server realtime/trade receipts require bounded `change_log` draining after each Redis wake plus the partial unsynced index. Health counts outstanding work; SMS blocks independently; only the audited reconciler may repair. Production remains Legacy without owner authorization.
- 2026-08-16 | Overtime writes, reconciliation, and sweepers run only on offer/request home; peer-mirror writes are rejected. Foreign/bot-home uses Telegram and Iran/WebApp-home presents in WebApp. Requester interaction stays on its source; cross-server cancellation is signed, fail-closed, idempotent, and forwarded home. Local Telegram receipts never bump/sync ledger version. Preference saves show only the result; never restore the retired cross-surface warning.
- 2026-08-12 | The 500-offer matrix uses 60/40 Bot/WebApp origin, random 0.8–4-second ingress, fake private transport, bounded lifecycle work, and redacted audits. Synthetic unknown-client quarantine proves queue execution—not delivery—and smoke cleanup is unconditional.
- 2026-08-11 | Queue-v1 uses central ingress, durable B2B receipts, and sticky first-publish ownership across five identities; callbacks stay receiver-local. Publication scans skip non-final jobs. Only pre-dispatch serialization/deadlock aborts retry under the fenced cadence lease; other DB failures fail closed. Dispatch-guard freshness changes are expected fenced requeues, not job errors.
- 2026-08-11 | Interaction probes require authenticated private chat and positive message identity. Resume clears shared destination cadence and only preflight-approved lane blocks.
