# Telegram Delivery

- 2026-08-18 | Cross-server numeric offer IDs are local: lot suggestions rebind to the source mirror and retain `offer_public_id` on API and bot paths. `overtime_delivering` stays queued under bounded fast polling; countdown starts from presentation/deadline and remaining-only snapshots decay locally. Profile reachability uses `telegram_id`, never `has_bot_access`; username is optional (`tg://user?id=` fallback), while unlinked stale usernames remain unrecognized.
- 2026-08-17 | Telegram Mini App and `/api/auth/webapp-login` are retired product surfaces. Do not migrate, revive, provision API bot credentials for, or treat them as supported; current WebApp authentication is OTP-first.
- 2026-08-17 | Staging Telegram execution is Queue-v1 on the bot host; API/WebApp stay producer-only. Login OTP uses an encrypted bot-owned Redis stream with terminal ACK+DELETE, exact retry IDs, and idempotent poison quarantine; the HTTP gateway omits optional `None` fields. Central private ingress, one primary slot, private outbox and trade-result feeders poll at 200ms; other slots, publisher/editor cadence, and the destination limiter stay unchanged. Offer-shaped text always yields `OFFER_VALIDATION_RESPONSE`; publisher executors cover only offer actions and owner callbacks. Cross-server realtime/trade receipts require a bounded durable `change_log` drain after every Redis wake-up plus the partial unsynced-aggregate index—never one row per signal. Health counts outstanding work, SMS may block independently, and only the audited reconciler is allowed. Production stays Legacy pending owner authorization.
- 2026-08-16 | Overtime lifecycle writes, reconciliation, and timeout sweepers run only on offer/request home; that home rejects peer-mirror writes. Foreign/bot-home uses Telegram and Iran/WebApp-home presents in WebApp. Requester interaction stays on its source; cross-server cancellation is signed, fail-closed, idempotent, and forwarded home. Local Telegram requester-message receipts never bump or sync the ledger version.
- 2026-08-12 | The 500-offer matrix uses 60/40 Bot/WebApp origin, random 0.8–4-second ingress, fake private transport, bounded lifecycle work, and redacted audits.
- 2026-08-11 | Queue-v1 uses central ingress, durable B2B command/receipt and first-publish lane ownership across five capable identities; callbacks stay on the receiver and edit ownership cannot transfer. Publication scans skip existing non-final jobs. Only pre-dispatch serialization/deadlock aborts retry under the fenced cadence lease; other DB failures fail closed.
- 2026-08-11 | Interaction probes require authenticated private chat and positive message identity. Resume clears shared destination cadence and only preflight-approved lane blocks.
