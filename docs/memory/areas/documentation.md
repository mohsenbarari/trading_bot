# Documentation

- 2026-09-03 | `DOC-4` approved: one drill-tested runbook governs each provisioning, normal deploy, hotfix, partition deploy, migration, rollback/forward-fix, Writer transfer, reconnect, sync repair, restore/DR, model rebuild, rotation, patching, incident, and legacy-retirement operation. Every runbook separates human authorization from Codex/Ansible/controller execution and declares risk/connectivity/Writer prerequisites, time and outage bounds, resumable step IDs, expected evidence, stop/abort conditions, recovery, and audit. Dashboard and emergency CLI share one state machine; deploy never changes Writer/DNS.

- 2026-09-03 | `DOC-3` approved: contract documentation separates human semantic rules, executable schemas, and CI-generated references. A machine-readable registry identifies every contract's version, owner, producers/consumers, authority, schema source, compatibility, ordering, deduplication, retention, and tests. Web/Bot and Finland/Iran provenance and business differences remain explicit; schema evolution is expand-compatible-consumers-backfill-verify-contract, with duplicates idempotent, gaps reconciled, and unknown incompatible versions quarantined.

- 2026-09-03 | `DOC-2` approved: canonical architecture documentation comprises system overview, two-site topology, authority/state machine, service ownership matrix, data flow/sync, Market Intelligence, and security architecture. It must trace every service, job, side effect, data stream, and connected/partitioned/reconnecting/deploy scenario to code, contracts, ADRs, and tests; Mermaid diagrams remain diffable. Mixed legacy documents are decomposed only after unique valid content is transferred and verified.

- 2026-09-03 | `DOC-1` approved: refactor documentation around a root `README.md`, `docs/INDEX.md`, and canonical architecture, contracts, operations/runbooks, testing, refactor, ADR, and bounded archive areas. Each active topic has exactly one source of truth with status, owner, applicability, review date/triggers, and supersession metadata; canonical filenames are not date-stamped.

- 2026-09-03 | Cursor must inventory every current document as `KEEP`, `MERGE`, `ARCHIVE`, or `DELETE`, build an old-to-canonical trace map, consolidate unique valid content, and validate links, commands, versions, and code alignment before removal. Generated logs and bulky evidence are not documentation and leave Git for retention-controlled storage. Archive keeps only uniquely useful history with an owner and review/expiry policy, never indefinite backup copies.
