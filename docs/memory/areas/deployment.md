# Deployment

- 2026-09-03 | `DPL-6` approved: replace shared production `.env` files with a signed typed config contract, Ansible-managed site config, least-privilege per-service secret files, and controller-owned operational state. Both sites run one artifact digest; config-only revisions are signed, compatibility-bound, standby-first and rollbackable with their release. Missing critical config blocks activation without disturbing the current runtime; secrets never sync through Git, artifacts, Object Storage, application replication, logs, or dashboards.

- 2026-09-03 | `DPL-5` approved: each host owns an idempotent local deploy controller and journal. When connected, stage both hosts and activate standby before Writer; compatible R0/R1 may deploy locally during a partition as `DIVERGED_SAFE`. A second-host failure restores the last converged release, while link loss during activation is resolved from local smoke results. Writer transfer requires parity only for shared role-relevant components, not Finland-only Bot/Executor. Either dashboard may start one mutex-bound deployment; deploy never changes Writer/DNS.

- 2026-09-03 | `DPL-4` approved: migrations leave app startup and use signed runners plus explicit expand/backfill/contract metadata. Only proven `partition_safe` additive migrations may run while partitioned. R3 needs synchronized sites, continuous WAL, daily base backup, weekly restore-smoke, monthly recovery drill, maintenance responses, and case-specific downtime approval. Post-R3 failure is human forward-fix or exact two-site restore, never destructive auto-rollback.

- 2026-09-03 | `DPL-3` approved: CI builds/tests exact content-addressed artifacts once and reuses tree/digest/policy evidence; activation stays human. Content evidence expires on drift; security=24h, environment=5m, live ownership/sync=30s. Hotfix may finish the full matrix after activation. Only noncritical tests allow owner-bound 72h risk acceptance; asynchronous CI failure freezes promotion, while runtime smoke auto-rolls back.

- 2026-09-03 | `DPL-2` approved: production runs signed immutable Ed25519 artifacts with transport-independent manual import. Same-host Docker/Nginx Blue/Green applies to Web/API; jobs become singleton, while Bot/Executor and Sync use single-owner handoffs. Prior slots stay warm 5m normally/2m for hotfix; compatible smoke failures auto-rollback. Active/previous releases and bounded retention are mandatory.

- 2026-09-03 | `DPL-1` approved: plans classify components, R0-R4 risk, Writer/connectivity, and urgency; ambiguity expands scope. Non-schema hotfixes retain immutable `main` provenance and target <=10m. Deploy never changes Writer/DNS. Connected deploys are standby-first; compatible partition versions may diverge, but Writer transfer waits for parity and sync.
