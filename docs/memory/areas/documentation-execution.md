# Documentation Execution

- 2026-09-03 | Branch clarification: consolidation means merging the latest `main` into `plan/two-server-refactor-v1`, so the plan branch contains all current `main` history plus its plan-only commits. It does not authorize repointing or pushing `main`, `origin/main`, or `candidate/wa-ir-standby-v1`; those refs remain unchanged unless separately requested.

- 2026-09-03 | `DOC-10` approved: package the five-section plan as canonical human `MASTER_PLAN.md`, machine-only dependency/order YAML, per-section stage documents, change ledger, and report/gap/decision templates. Add one manually invoked `.cursor/skills/two-site-refactor/SKILL.md` (`disable-model-invocation: true`) plus short scoped `.mdc` guardrails; neither duplicates canonical details. One temporary `refactor/two-site-architecture` branch and checkout executes one dependency-ready stage at a time, with baseline, bounded scope, tests/evidence, rollback, Persian report, user-only approval, and hard stops before production, migration, DNS/Writer, secret, destructive, or legacy-retirement actions.

- 2026-09-03 | `DOC-9` approved: canonical narrative is Persian with English identifiers/schemas and no duplicate translation. A catalog governs `DRAFT/ACTIVE/SUPERSEDED/ARCHIVE/DELETE`, ownership, source-of-truth scope, code/contracts/tests, review triggers, supersession, and expiry. PRs declare documentation impact; fast CI hard-gates metadata, internal links, unique IDs/authority, referenced paths, secrets, generated-schema drift, and Mermaid, targeting <=60s for hotfixes, while external/staleness/archive audits run asynchronously. Superseded stubs/backups are forbidden; bounded retention ends support documents, roadmaps, ADR history, release notes, mappings, and unreferenced assets.

- 2026-09-03 | `DOC-8` approved: provide canonical getting-started, codebase map, local environments, typed configuration reference, command reference, database/migrations, debugging, repository hygiene, and contribution documents. A fresh `main` checkout follows tested `doctor/bootstrap/dev-up/test-fast` entrypoints without production credentials or hidden external files. Local disposable logs/results/cache/scratch live under ignored, retention-managed `var/`; production immutable releases remain separate from shared state, secure material, and runtime var. Existing assistant/structure/dev-tool guides are merged or generated, never parallel authorities.
