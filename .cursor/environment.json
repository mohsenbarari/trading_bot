{
  "name": "Trading Bot (native dev)",
  "user": "ubuntu",
  "install": "bash .cursor/install.sh",
  "start": "bash .cursor/start.sh",
  "terminals": [
    {
      "name": "backend-api",
      "command": "cd /workspace && source .venv/bin/activate && set -a && . ./.env && set +a && exec uvicorn main:app --host 0.0.0.0 --port 8000 --reload"
    },
    {
      "name": "frontend-vite",
      "command": "cd /workspace/frontend && exec npx vite --host 0.0.0.0 --port 5173"
    }
  ],
  "ports": [
    { "name": "Backend API", "port": 8000 },
    { "name": "Frontend (Vite)", "port": 5173 }
  ]
}
